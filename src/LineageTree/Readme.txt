It runs the model the first time, if the file simulation_data.pkl is not in the folder, after that it just upload the file.

plot_lineage_tree.py:  plots and saves the figure
Needs hematopoiesis_model_v5.py in the same folder

plot_hierarchical_tree_gui.py plots trees for different cell IDs, doesn't run the code jsut upload the data